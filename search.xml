<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>《ArrayList的底层原理》</title>
      <link href="/2025/06/03/2025060319/"/>
      <url>/2025/06/03/2025060319/</url>
      
        <content type="html"><![CDATA[<h2 id="简介">简介</h2><p><strong>ArrayList</strong> 是 Java 中常用的动态数组实现，是基于<strong>数组实现的可变长度集合</strong>，可以看作是一个动态数组结构，通过一个内部数组 <code>Object[] elementData</code>存储元素。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 默认构造函数</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ArrayList</span><span class="params">()</span> &#123;</span><br><span class="line">    <span class="built_in">this</span>.elementData = DEFAULTCAPACITY_EMPTY_ELEMENTDATA;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">// 指定初始容量</span></span><br><span class="line"><span class="keyword">public</span> <span class="title function_">ArrayList</span><span class="params">(<span class="type">int</span> initialCapacity)</span> &#123;</span><br><span class="line">    <span class="keyword">if</span> (initialCapacity &gt; <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">this</span>.elementData = <span class="keyword">new</span> <span class="title class_">Object</span>[initialCapacity];</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (initialCapacity == <span class="number">0</span>) &#123;</span><br><span class="line">        <span class="built_in">this</span>.elementData = EMPTY_ELEMENTDATA;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        <span class="keyword">throw</span> <span class="keyword">new</span> <span class="title class_">IllegalArgumentException</span>(<span class="string">&quot;Illegal Capacity: &quot;</span>+</span><br><span class="line">                                           initialCapacity);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="扩容机制">扩容机制</h2><p>扩容机制是 ArrayList 的核心，ArrayList 默认的初始容量为 10，当数组容量不够时，会进行扩容。扩容的机制是：在添加元素时，ArrayList 会先判断数组是否已满，已满则调用 <code>ensureCapacityInternal</code> 方法进行扩容。</p><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">ensureCapacityInternal</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">    <span class="comment">// 如果是第一次添加元素，默认容量是 10</span></span><br><span class="line">    <span class="keyword">if</span> (elementData == DEFAULTCAPACITY_EMPTY_ELEMENTDATA) &#123;</span><br><span class="line">        minCapacity = Math.max(DEFAULT_CAPACITY, minCapacity);</span><br><span class="line">    &#125;</span><br><span class="line">    ensureExplicitCapacity(minCapacity);</span><br><span class="line">&#125;</span><br><span class="line">    <span class="comment">// 判断是否需要扩容</span></span><br><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">ensureExplicitCapacity</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">    modCount++; <span class="comment">// 修改次数 +1，配合 fail-fast</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// 如果当前最小容量大于数组长度，就扩容</span></span><br><span class="line">    <span class="keyword">if</span> (minCapacity - elementData.length &gt; <span class="number">0</span>)</span><br><span class="line">        grow(minCapacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="核心扩容方法">核心扩容方法</h3><figure class="highlight java"><table><tr><td class="code"><pre><span class="line"><span class="keyword">private</span> <span class="keyword">void</span> <span class="title function_">grow</span><span class="params">(<span class="type">int</span> minCapacity)</span> &#123;</span><br><span class="line">    <span class="type">int</span> <span class="variable">oldCapacity</span> <span class="operator">=</span> elementData.length;</span><br><span class="line">    <span class="comment">// 扩容为原容量的 1.5 倍</span></span><br><span class="line">    <span class="type">int</span> <span class="variable">newCapacity</span> <span class="operator">=</span> oldCapacity + (oldCapacity &gt;&gt; <span class="number">1</span>);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果新容量仍然不够，就用最小需要容量</span></span><br><span class="line">    <span class="keyword">if</span> (newCapacity - minCapacity &lt; <span class="number">0</span>)</span><br><span class="line">        newCapacity = minCapacity;</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 如果超过最大容量限制</span></span><br><span class="line">    <span class="keyword">if</span> (newCapacity - MAX_ARRAY_SIZE &gt; <span class="number">0</span>)</span><br><span class="line">        newCapacity = hugeCapacity(minCapacity);</span><br><span class="line">    </span><br><span class="line">    <span class="comment">// 复制新数组</span></span><br><span class="line">    elementData = Arrays.copyOf(elementData, newCapacity);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
      
      
      
        <tags>
            
            <tag> Java基础 </tag>
            
            <tag> 数据结构 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>《素笺初展时》</title>
      <link href="/2025/06/03/start/"/>
      <url>/2025/06/03/start/</url>
      
        <content type="html"><![CDATA[<h2 id="启程时刻-✨">启程时刻 ✨</h2><p><strong>2025 年 6 月 3 日</strong>，博客的第一页在此展开。</p><blockquote><p>像翻开一本新日记的扉页，<br>干净而安静。<br>键盘尚未温热，页面素净如雪，<br>宛如新铺的小径，<br>等待足迹的落下，思绪的绵延。</p></blockquote><h3 id="避风角落-🏮">避风角落 🏮</h3><p>这里不是喧嚣之地，<br>而是：</p><ul><li>记录想法的 📖 记事本</li><li>分享灵感的 💡 储藏间</li><li>偶尔碎语的 🌙 夜光灯</li></ul><h3 id="慢写宣言-🖋️">慢写宣言 🖋️</h3><p>在这个快转的世界里，<br>我们选择：</p><ol><li><strong>给文字以呼吸的空间</strong></li><li><strong>为心情保留回望的窗口</strong></li><li><strong>替未来的自己点亮一盏灯</strong></li></ol><blockquote><p>“若无一丝错落，<br>人生怎会如诗？”</p></blockquote><hr><p>🌱 <strong>从今日起</strong>，与博客共生长。<br>🫂 <strong>欢迎你</strong>，愿常常相逢。</p><p>—— 记于起点</p>]]></content>
      
      
      
        <tags>
            
            <tag> 生活 </tag>
            
            <tag> 思考 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
